# Session Log - 24/12/2025

## Task: Teacher Co-op Courses Display

### Problem
Admin panel hiển thị "0 khóa học" cho các giảng viên được mời co-op, mặc dù họ thực sự có khóa học cộng tác.

### Root Cause Discovery
- **Sai**: Dùng `teachingStaffIds` (Set<UUID> trong Course entity)
- **Đúng**: Dữ liệu lưu trong bảng `course_instructors` với entity `CourseInstructor`

### Data Model
```java
@Entity
@Table(name = "course_instructors")
public class CourseInstructor {
    Course course;
    User user;
    InstructorRole role;    // OWNER | CO_INSTRUCTOR
    InstructorStatus status; // PENDING | ACCEPTED | REJECTED | REMOVED
}
```

### Backend Changes
1. **CourseInstructorRepository**: Thêm `countCoopCoursesByUserId(UUID)`
2. **AdminUserDTO**: Thêm `coursesCooped` field
3. **UserService**: `countCoursesCoopedByUser()` dùng CourseInstructorRepository
4. **AdminController**: Endpoint `GET /users/{id}/coop-courses`

### Frontend Changes
1. **AdminUser interface**: Thêm `coursesCooped`
2. **Teacher table**: Hiển thị "X sở hữu" + "Y cộng tác"
3. **Modal**: 2 sections - Purple (owned), Blue (co-op)

### Files Modified
- `api/src/main/java/com/example/lms/repository/CourseInstructorRepository.java`
- `api/src/main/java/com/example/lms/dto/AdminUserDTO.java`
- `api/src/main/java/com/example/lms/service/UserService.java`
- `api/src/main/java/com/example/lms/controller/AdminController.java`
- `fe/src/app/features/admin/infrastructure/services/admin.service.ts`
- `fe/src/app/features/admin/presentation/components/teacher-management.component.ts`

### Build Status
✅ Compilation SUCCESS
